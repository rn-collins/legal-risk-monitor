{
  "name": "legal-risk-monitor",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "build": "node scripts/render.mjs && node scripts/validate.mjs"
  }
}
